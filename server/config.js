export const isProd = process.env.NODE_ENV === 'production';

export const PORT = process.env.PORT || 5000;
export const IS_DELAY_ENABLED = process.env.IS_DELAY_ENABLED || false;
export const DELAY = process.env.DELAY || 0;
